#include<stdio.h>
int main()
{
	int p,r,t,int_amt;
	printf("input principle,rate of intrest and time to find simple intrest: \n");
	scanf("%d%d%d",&p,&r,&t);
	int_amt = (p*r*t)/100;
	printf("simple intrest = %d",int_amt);
	return 0;
}