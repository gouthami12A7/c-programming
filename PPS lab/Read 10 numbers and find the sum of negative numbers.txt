//Read 10 numbers and find the sum of negative numbers
#include<stdio.h>
int main()
{
	int a,sum,n;
	printf("\n Enter the number:");
	n = 0;
	sum = 0;
	do
	{
		scanf("%d",&a);
		if(a<0)
		sum = sum+n;
		n = n +1;
	}
	while(n<10);
	printf("the sum of negative numbers = %d",sum);
}