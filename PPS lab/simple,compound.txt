/*Write the program for the simple, compound interest.*/

#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
	float p,t,r,si,ci;
	int clrscr();
	printf("enter principle amount(p):");
	scanf("%f",&p);
	printf("enter time in year(t):");
	scanf("%f",&t);
	printf("enter rate in percent(r):");
	scanf("%f",&r);
	si=(p*t*r)/100.0;
	ci=p*((1+r/100)*t);
	printf("simple interest=%0.3f\n",si);
	printf("coumpound interest=%0.3f",ci);
	return 0;
}