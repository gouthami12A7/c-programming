//Count the digits of a given number
#include<stdio.h>
int main()
{
	int x,y,count;
	count = 0;
	printf("Enter the number:");
	scanf("%d",&x);
	while(x>=1)
	{
		y=x%10;
		count=count+1;
		x=x/10;
	}
	printf("number of digits = %d",count);
}